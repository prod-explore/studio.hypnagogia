server {
    listen 80;
    server_name studio.hypnagogia.pl;
    root /usr/share/nginx/html;
    index index.html;
    charset utf-8;

    add_header Cache-Control "public, max-age=3600";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ =404;
    }
}
